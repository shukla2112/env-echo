apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "env-echo.fullname" . }}-test-connection"
  labels:
    {{- include "env-echo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "env-echo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
