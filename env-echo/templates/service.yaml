apiVersion: v1
kind: Service
metadata:
  name: {{ include "env-echo.fullname" . }}
  labels:
    {{- include "env-echo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "env-echo.selectorLabels" . | nindent 4 }}
